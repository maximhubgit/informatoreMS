/// Costanti dell'applicazione.
class AppConstants {
  AppConstants._();

  static const String appName = 'BIOGENA';

  // Durata di default in minuti per una visita.
  static const int defaultTempoVisitaMinuti = 30;

  // Numero massimo di appuntamenti per giorno per medico.
  static const int defaultMaxAppuntamentiGiorno = 1;
}
